# 실시간 지식 그래프 (Real-time Knowledge Graph)

> 온라인 회의, 강의, 음성 콘텐츠를 실시간으로 분석하여 지식 그래프로 시각화하는 데스크톱 애플리케이션

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20macOS-lightgrey)

## 🎯 주요 기능

- **실시간 시스템 오디오 캡처** - Electron desktopCapturer를 통한 시스템 사운드 캡처
- **Gemini 기반 STT** - Google Gemini API를 사용한 한국어 음성 인식
- **Kiwi 형태소 분석** - 한국어 NLP로 정확한 문장 단위 분석
- **LLM 지식 추출** - LangChain + Gemini를 통한 엔티티/관계 추출
- **실시간 그래프 시각화** - React Flow 기반 인터랙티브 지식 그래프

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Electron Desktop App                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Audio     │  │  WebSocket  │  │    React Flow       │  │
│  │   Capture   │──│   Client    │──│    Visualization    │  │
│  └─────────────┘  └──────┬──────┘  └─────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │ WebSocket
┌─────────────────────────▼───────────────────────────────────┐
│                    FastAPI Server                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   STT       │  │    NLP      │  │    Extraction       │  │
│  │  (Gemini)   │──│   (Kiwi)    │──│   (LangChain)       │  │
│  └─────────────┘  └─────────────┘  └──────────┬──────────┘  │
│  ┌─────────────────────────────────────────────▼──────────┐  │
│  │                  Graph State Manager                    │  │
│  └─────────────────────────────────────────────┬──────────┘  │
└─────────────────────────────────────────────────┬───────────┘
                                                  │
┌─────────────────────────────────────────────────▼───────────┐
│                         Redis                                │
│  • Audio Buffer (Sorted Set)                                 │
│  • STT Buffer (List)                                         │
│  • Extraction Queue (Stream)                                 │
│  • Graph State (JSON)                                        │
└─────────────────────────────────────────────────────────────┘
```

## 📁 프로젝트 구조

```
/realtime-knowledge-graph
├── /apps
│   └── /desktop              # Electron + React 데스크톱 앱
│       ├── /electron         # Electron 메인 프로세스
│       └── /src              # React 렌더러
│           ├── /components   # UI 컴포넌트
│           ├── /hooks        # React 훅
│           └── /store        # Zustand 상태 관리
├── /servers
│   └── /api                  # FastAPI 백엔드 서버
│       ├── main.py          # 엔트리포인트
│       ├── websocket.py     # WebSocket 핸들러
│       ├── stt.py           # Gemini STT
│       ├── nlp.py           # Kiwi 형태소 분석
│       ├── extraction.py    # LLM 지식 추출
│       └── graph_state.py   # 그래프 상태 관리
├── /packages
│   ├── /shared-types        # 공유 TypeScript/Pydantic 타입
│   ├── /llm-prompts         # LLM 프롬프트 버전 관리
│   └── /graph-utils         # 그래프 유틸리티
└── /infra
    ├── docker-compose.yml   # 프로덕션 Docker 설정
    └── redis.conf           # Redis 설정
```

## 🚀 빠른 시작

### 사전 요구사항

- Node.js >= 18.x
- Python >= 3.11
- Redis >= 7.x
- Gemini API 키

### 1. 저장소 클론 및 의존성 설치

```bash
# 클론
git clone <repository-url>
cd realtime-knowledge-graph

# Node.js 의존성 설치 (루트)
yarn install

# Python 가상환경 및 의존성 설치
cd servers/api
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. 환경 변수 설정

```bash
# servers/api/.env 생성
GEMINI_API_KEY=your_gemini_api_key_here
REDIS_URL=redis://localhost:6379/0
LOG_LEVEL=INFO
```

### 3. Redis 실행

```bash
# Docker 사용 (권장)
cd infra
docker-compose -f docker-compose.dev.yml up -d

# 또는 로컬 Redis 실행
redis-server
```

### 4. API 서버 실행

```bash
cd servers/api
source venv/bin/activate
python main.py

# 또는 uvicorn으로 실행
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 5. 데스크톱 앱 실행

```bash
# 루트 디렉토리에서
yarn dev:desktop

# 또는 앱 디렉토리에서
cd apps/desktop
yarn dev
```

## 📖 사용 방법

1. **앱 시작**: 데스크톱 앱을 실행하면 자동으로 서버에 연결됩니다.
2. **오디오 캡처 시작**: "오디오 캡처 시작" 버튼을 클릭합니다.
3. **화면/오디오 선택**: 시스템 오디오를 캡처할 화면을 선택합니다.
4. **실시간 분석**: 음성이 실시간으로 텍스트로 변환되고, 지식 그래프가 생성됩니다.
5. **그래프 탐색**: 생성된 그래프를 드래그, 줌, 패닝으로 탐색합니다.

## ⚙️ 설정

### API 서버 설정 (config.py)

| 환경 변수 | 설명 | 기본값 |
|-----------|------|--------|
| `GEMINI_API_KEY` | Gemini API 키 | - |
| `REDIS_URL` | Redis 연결 URL | `redis://localhost:6379/0` |
| `LOG_LEVEL` | 로그 레벨 | `INFO` |
| `STT_CHUNK_DURATION_MS` | STT 청크 크기 (ms) | `1000` |
| `EXTRACTION_BATCH_SIZE` | 추출 배치 크기 | `3` |

### 그래프 커스터마이징

엔티티 타입별 색상은 `apps/desktop/src/store/graphStore.ts`에서 수정 가능합니다.

## 🛠️ 개발

### 공유 패키지 빌드

```bash
# 타입 패키지 빌드
yarn workspace @rkg/shared-types build

# 프롬프트 패키지 빌드
yarn workspace @rkg/llm-prompts build

# 그래프 유틸 빌드
yarn workspace @rkg/graph-utils build
```

### 린트 및 타입체크

```bash
# TypeScript 타입체크
yarn typecheck

# ESLint
yarn lint

# Python (서버)
cd servers/api
ruff check .
mypy .
```

### 프로덕션 빌드

```bash
# 데스크톱 앱 빌드
yarn build:desktop

# Docker 이미지 빌드
cd infra
docker-compose build
```

## 🔧 문제 해결

### "Redis 연결 실패"

Redis가 실행 중인지 확인하세요:
```bash
redis-cli ping  # PONG이 반환되어야 함
```

### "Gemini API 오류"

- API 키가 올바른지 확인
- API 할당량이 초과되지 않았는지 확인
- 네트워크 연결 확인

### "오디오 캡처 실패"

- Windows: "스테레오 믹스"가 활성화되어 있는지 확인
- macOS: 화면 녹화 권한이 부여되었는지 확인
- 시스템 오디오 출력이 설정되어 있는지 확인

## 📄 라이선스

MIT License

## 🤝 기여

Pull Request와 Issue는 언제나 환영합니다!

---

**Made with ❤️ for the Korean market**



